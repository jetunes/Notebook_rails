<%= form_for(@adrress, html:{class: "basic-grey"}) do |f| %>
  <% if @adrress.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adrress.errors.count, "error") %> prohibited this adrress from being saved:</h2>

      <ul>
      <% @adrress.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h1>Endereço</h1>>

  <div class="field">
    <%= f.label :street %><br>
    <%= f.text_field :street %>
  </div>
  <div class="field">
    <%= f.label :city %><br>
    <%= f.text_field :city %>
  </div>
  <div class="field">
    <%= f.label :state %><br>
    <%= f.text_field :state %>
  </div>
  <div class="field">
    <%= f.label :contact_id %><br>
    <%= f.text_field :contact_id %>
  </div>
  <div class="actions">
    <%= f.submit "Criar", class: "button" %>
    <%= link_to 'Back', adrresses_path, class: "button-others" %>
  </div>
<% end %>
